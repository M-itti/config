#!/bin/bash

# Replace "Wi-Fi" with your network service name if different
SERVICE="Wi-Fi"

# Set DNS servers
sudo networksetup -setdnsservers "$SERVICE" 1.1.1.1 8.8.8.8

echo "DNS servers set to 1.1.1.1, 8.8.8.8 for $SERVICE"
